# 2026-05-18 Work Log

## Homepage Hero Banner Full-screen Carousel
- Replaced left-right split layout with full-screen background image carousel in `HeroSection.tsx`
- Added 4 banner slides with background images, dark overlay, centered text content
- Implemented auto-rotate (5s interval) with left/right arrows and dot indicators
- Fixed scroll lock issue caused by Tailwind preflight: added `html { overflow-y: scroll }` and `body { min-height: 100vh }` in `globals.css`
- Removed duplicate `<main>` nesting in `page.tsx`

## Navbar Updates
- Added `Home` nav item to Navbar with house icon
- Implemented dynamic active state using `usePathname()` from Next.js
- Home is blue-highlighted on `/`, Products on `/products`, Capabilities/About Us text turns blue on respective pages
